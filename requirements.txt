# Core dependencies -- required for the default single-machine setup
# (CLI + web UI, SQLite job store, in-process worker).
langgraph>=0.2
pydantic>=2
pdfplumber>=0.11
reportlab>=4
anthropic>=0.34
openai>=1.40
flask>=3.0
tqdm>=4.66

# --- Optional: multi-machine deployment (Postgres + Redis/RQ) ---
# Not needed for the default setup. Remove this block if you don't need it,
# or install everything as-is if you do.
# See README "Scaling to multiple machines".
psycopg2-binary>=2.9
redis>=5.0
rq>=1.16

# --- Optional: OCR for scanned/photographed reports ---
# Not needed unless you're processing scanned PDFs with no text layer.
# Also requires the tesseract-ocr binary itself, which pip can't install --
# see README "Scanned/photographed reports (OCR)" for install instructions.
pymupdf>=1.24
pytesseract>=0.3.10
Pillow>=10.0

# --- Optional: development/testing ---
pytest>=8
